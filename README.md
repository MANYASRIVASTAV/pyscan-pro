# pyscan-pro


A lightweight Python tool to analyze large projects and detect common errors.

It scans your project folder, counts files and lines of code, and finds issues like syntax errors and undefined variables before runtime.

---

## 🚀 Features

- 📂 Scan any project directory
- 📄 Count total Python files
- 📊 Count total lines of code
- ❌ Detect syntax errors
- ⚠️ Find undefined variables/functions (e.g., typos like `lprint`)
- 📍 Show file name and line number
- ⚡ Works with large projects (10k+ lines)

---

## 🛠️ Requirements

- Python 3.8 or higher
- No external libraries required


